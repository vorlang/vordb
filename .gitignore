directions/

# Gleam build
/build/

# Data
/data/

# Bench build artifacts (ebin only — keep src + Makefile + results)
/bench/ebin/

# Crash dumps
erl_crash.dump

# Env
.env

# Elixir (legacy — removed)
/_build/
/deps/
